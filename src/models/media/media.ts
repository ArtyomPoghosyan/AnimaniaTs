export interface IMainVideo { 
    isLoading: boolean,
    isSuccess: boolean,
    userData: [],
    userError:null
}

export interface IVideo {
    createdAt:string,
    id:number,
    path:string,
    updatedAt:string
}